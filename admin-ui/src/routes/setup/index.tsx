import SetupForm from "../../components/setup/SetupForm";

export default function SetupPage() {
  return (
    <main class="flex items-center justify-between h-dvh max-w-md mx-auto p-4">
      <SetupForm />
    </main>
  );
}
